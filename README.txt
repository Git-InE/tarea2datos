Utilizamos en Windows el subsistema Linux version () para compilar el código con el comando (comando)"

Los archivos:
 
-"tarea2.cpp" 
-"super_string.c++"

En el archivo "tarea2.cpp" se encuentra el main del programa.

Deben estar en las misma carpeta al momento de ejecutar el codigo.